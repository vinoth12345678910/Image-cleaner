name: image_cleaner
description: "A Flutter image cleaner app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  hive_flutter: ^1.1.0   # add this
  # your existing deps stay as is
  flutter:
    sdk: flutter
  google_mlkit_text_recognition: ^0.13.0
  image: ^4.3.0
  crypto: ^3.0.3
  permission_handler: ^11.3.1
  photo_manager_image_provider: ^2.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true